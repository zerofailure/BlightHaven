export function addItemToInventory(inventory, itemId, quantityToAdd) { const item = inventory.find(i => i.itemId === itemId); if (item) { item.quantity = Math.min(item.quantity + quantityToAdd, 999999999); } else { inventory.push({ itemId, quantity: quantityToAdd }); } return inventory; }