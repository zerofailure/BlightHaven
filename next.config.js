/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the experimental.serverActions option - it's no longer needed
  // Server Actions are enabled by default in Next.js 14+
}

module.exports = nextConfig
